# Type Hinting
- What something might be
- Annotations
- Improves code readability, maintainability


# (Static) Type Checking
- Static Analysis (sticking to declared types)
- Anaylzes code before runtime, catches errors
- Requires external tool (mypy)
- Does not prevent running the code

# Data Validation
- Happens at runtime, data meets requirements
- During code execution
- Can raise validation errors
- pydantic
